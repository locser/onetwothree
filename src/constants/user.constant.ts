export enum USER_ROLE {
  MEMBER = 'MEMBER',
  MOD = 'MOD',
  ADMIN = 'ADMIN',
}

export enum USER_RANK {
  NO_RANK = 'NO_RANK',
  BRONZE = 'BRONZE',
  SILVER = 'SILVER',
  GOLD = 'GOLD',
}

export enum USER_STATUS {
  NOT_ACTIVE = 0,
  ACTIVE = 1,
}

export const PASSWORD_MIN_LENGTH = 6;
